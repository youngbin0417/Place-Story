package com.example.diaryprogram.data

enum class DiaryStatus {
    PUBLIC, PRIVATE, FOLLOWER // These should match server-side enum values exactly.
}

enum class ImageType{
    USER_PROFILE, DIARY_IMAGE
}
